apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mihir-uk-tls
  namespace: default

spec:
  secretName: mihir-uk-tls

  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer

  dnsNames:
    - makemycv.mihir.uk
    - it-tools.mihir.uk